image: "r8.im/humaniacul/stable-makeup-pure"

build:
  gpu: true
  cuda: "11.8"
  python_version: "3.10"
  run:
    - "apt-get update && apt-get install -y --no-install-recommends git ffmpeg libgl1-mesa-glx libglib2.0-0 libsm6 libxext6 libxrender-dev libgomp1 && rm -rf /var/lib/apt/lists/*"

  


predict: "predict.py:Predictor" 